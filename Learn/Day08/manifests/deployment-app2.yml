apiVersion: apps/v1
kind: Deployment
metadata:
  name: app2
  namespace: k8s-day08
  labels:
    app: app2
    day: "08"
  annotations:
    kubernetes.io/change-cause: "day-08 app2 for ingress routing"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: app2
  template:
    metadata:
      labels:
        app: app2
    spec:
      containers:
        - name: nginx
          image: nginx:stable
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "100m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: app2-service
  namespace: k8s-day08
spec:
  selector:
    app: app2
  ports:
    - port: 80
      targetPort: 80
