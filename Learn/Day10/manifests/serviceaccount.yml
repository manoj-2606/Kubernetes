apiVersion: v1
kind: ServiceAccount
metadata:
  name: workload-sa
  namespace: day10
  labels:
    day: "10"
  annotations:
    azure.workload.identity/client-id: "<REPLACE_WITH_MANAGED_IDENTITY_CLIENT_ID>"
